class DynamicProgramming {
	static Void main() {

		Num n := 2
		// n.mult(2)
		// Unknown method 'sys::Num.mult'
		n->mult(2)

		Obj o := "4"
		Int i1 := Int.fromStr((Str) o)
		Int i2 := Int.fromStr(o)
	}
}
